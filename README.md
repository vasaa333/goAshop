# 🛍️ goAshop - Telegram Bot Shop

Профессиональный Telegram-бот для управления интернет-магазином с системой закладок, админ-панелью и полным функционалом управления заказами.

## 📋 Возможности

### Для пользователей:
- 🛒 **Каталог товаров** с фильтрацией по городу и району
- ⚖️ **Гибкие веса** - поддержка дробных значений (0.25, 0.5 и т.д.)
- 💳 **Оплата** - загрузка чека, подтверждение администратором
- 📦 **Мои заказы** - просмотр истории с детальной информацией
- ⭐️ **Отзывы** - возможность оставить отзыв после покупки
- 🤖 **Капча** - защита от ботов (опционально)
- ℹ️ **Информация** - правила использования

### Для администратора:
- 📊 **Статистика** - полная аналитика по продажам
- 📦 **Управление заказами** - подтверждение/отклонение с уведомлениями
- 🗂 **Витрина** - управление товарами, городами, районами, инвентарем
- 👥 **Пользователи** - просмотр, блокировка, статистика
- 📢 **Рассылки** - массовая отправка сообщений
- ⚙️ **Настройки** - капча, тех.работы, приветствие, реквизиты, контакты
- 💾 **Экспорт/Импорт** - резервное копирование и восстановление
- 📋 **Логи** - просмотр последних 70 строк логов бота
- ⭐️ **Модерация отзывов** - одобрение/отклонение отзывов пользователей

## 🚀 Быстрый старт с НУЛЯ

### Шаг 1: Требования

Убедитесь, что у вас установлено:
- Python 3.8+ 
- pip (менеджер пакетов Python)
- git

```bash
# Проверка версий
python3 --version
pip3 --version
git --version
```

### Шаг 2: Клонирование репозитория

```bash
# Клонируем проект
git clone https://github.com/vasaa333/final.git
cd final
```

### Шаг 3: Установка зависимостей

```bash
# Устанавливаем необходимые библиотеки
pip3 install -r requirements.txt
```

### Шаг 4: Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям, задайте имя и username бота
4. Скопируйте полученный **API Token** (выглядит примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 5: Получение вашего Telegram ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему команду `/start`
3. Скопируйте ваш **ID** (например: `123456789`)

### Шаг 6: Настройка конфигурации

Создайте файл `.env` в корневой папке проекта:

```bash
# Создаём файл конфигурации
nano .env
```

Вставьте следующие параметры (замените на свои данные):

```env
# API токен вашего бота из BotFather
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш Telegram ID (для админ-доступа)
ADMIN_ID=123456789

# Имя файла базы данных (можно оставить по умолчанию)
DATABASE=bot.db
```

Сохраните файл:
- В `nano`: Ctrl+X, затем Y, затем Enter

### Шаг 7: Инициализация базы данных

```bash
# Запускаем миграцию БД (создание всех таблиц)
python3 db_migration.py
```

Вы должны увидеть:
```
✅ Миграция базы данных завершена успешно!
📊 База данных готова к работе с новым функционалом
```

### Шаг 8: Запуск бота

```bash
# Запуск в обычном режиме (видны все логи)
python3 bot.py
```

Или запуск в фоновом режиме:

```bash
# Запуск в фоне с записью логов
nohup python3 bot.py > bot.log 2>&1 &

# Проверка логов в реальном времени
tail -f bot.log
```

### Шаг 9: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте команду `/start`
4. Если всё настроено правильно, бот ответит приветственным сообщением

**Поздравляем! Ваш бот запущен! 🎉**

---

## 📚 Подробная документация

### Структура проекта

```
final/
├── bot.py                    # Главный файл бота с middleware
├── db_migration.py           # Миграции базы данных
├── message_handler.py        # Обработка покупок
├── admin_panel.py           # Главная админ-панель
├── admin_orders.py          # Управление заказами
├── admin_showcase.py        # Управление витриной
├── admin_users.py           # Управление пользователями
├── admin_broadcast.py       # Массовые рассылки
├── admin_settings.py        # Настройки бота
├── admin_logs_final.py      # Просмотр логов
├── user_menu.py             # Пользовательское меню
├── requirements.txt         # Зависимости Python
├── .env.example             # Пример конфигурации
├── .gitignore              # Исключения для git
└── README.md               # Этот файл
```

### Настройка через админ-панель

После запуска бота зайдите в него как администратор:

1. **Добавьте города и районы:**
   - Админ → Витрина → Города/Районы
   
2. **Создайте товары:**
   - Админ → Витрина → Товары → Добавить товар
   
3. **Добавьте инвентарь:**
   - Админ → Витрина → Наличие → Добавить позицию
   - Укажите город, район, вес, цену, зашифрованные данные
   
4. **Настройте бота:**
   - Админ → Настройки
   - Установите приветственное сообщение
   - Укажите реквизиты для оплаты
   - Добавьте контакт поддержки
   - Включите капчу при необходимости

### Режим тех.работ

Включение режима тех.работ блокирует доступ всем пользователям (кроме админа):

```
Админ → Настройки → 🛠 Тех.работы: 🟢 Вкл
```

Пользователи будут видеть сообщение:
```
🛠 Проводятся технические работы
Бот временно недоступен. Попробуйте позже.
```

### Капча

Защита от ботов с математическим вопросом:

```
Админ → Настройки → 🤖 Капча: 🟢 Вкл
```

При старте пользователь должен решить пример:
```
🤖 Защита от ботов

Решите пример:
5 + 3 = ?

[6] [7] [8] [9]
```

### Экспорт/Импорт данных

**Экспорт:**
```
Админ → Настройки → 💾 Сохранить настройки в файл
```

Создаётся JSON файл со всеми данными:
- Настройки бота
- Города и районы
- Товары и инвентарь
- Заказы
- Пользователи

**Импорт:**
```
1. Включите режим тех.работ
2. Админ → Настройки → 📲 Импорт настроек из файла
3. Отправьте JSON файл
```

### Просмотр логов

```
Админ → Логи → 📄 Последние 70 строк
```

Логи отображаются методом цитирования (блок кода) для удобства чтения.

---

## 🔧 Управление сервером

### Остановка бота

```bash
# Найти процесс
ps aux | grep bot.py

# Остановить по PID
kill <PID>

# Или остановить все процессы Python с ботом
pkill -f "python3 bot.py"
```

### Перезапуск бота

```bash
# Остановить
pkill -f "python3 bot.py"

# Запустить снова
nohup python3 bot.py > bot.log 2>&1 &
```

### Обновление кода

```bash
# Остановить бота
pkill -f "python3 bot.py"

# Получить новую версию
git pull origin main

# Обновить зависимости (если были изменения)
pip3 install -r requirements.txt

# Применить миграции БД (если были изменения)
python3 db_migration.py

# Запустить бота
nohup python3 bot.py > bot.log 2>&1 &
```

### Автозапуск через systemd

Создайте файл `/etc/systemd/system/goashop.service`:

```ini
[Unit]
Description=goAshop Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/path/to/final
Environment="PATH=/usr/bin"
ExecStart=/usr/bin/python3 /path/to/final/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
# Перезагрузить systemd
systemctl daemon-reload

# Включить автозапуск
systemctl enable goashop

# Запустить бота
systemctl start goashop

# Проверить статус
systemctl status goashop

# Просмотр логов
journalctl -u goashop -f
```

---

## 🐛 Решение проблем

### Бот не запускается

**Проверьте:**
1. Правильность токена в `.env`
2. Установлены ли все зависимости: `pip3 install -r requirements.txt`
3. Создана ли база данных: `python3 db_migration.py`

**Логи:**
```bash
# Посмотреть ошибки
cat bot.log | grep ERROR

# Последние 50 строк
tail -50 bot.log
```

### "Файл .env не найден"

```bash
# Проверьте наличие файла
ls -la .env

# Если нет - создайте
cp .env.example .env
nano .env
```

### "ModuleNotFoundError"

```bash
# Переустановите зависимости
pip3 install --upgrade -r requirements.txt
```

### Бот не отвечает

1. Проверьте, что процесс запущен:
```bash
ps aux | grep bot.py
```

2. Проверьте логи на ошибки:
```bash
tail -100 bot.log
```

3. Перезапустите бота:
```bash
pkill -f "python3 bot.py"
python3 bot.py
```

---

## 📝 Требования к системе

- **OS**: Linux (Ubuntu 20.04+, Debian 10+, CentOS 7+)
- **Python**: 3.8+
- **RAM**: минимум 256 MB
- **Disk**: минимум 500 MB свободного места
- **Network**: стабильное интернет-соединение

---

## 📦 Зависимости

```
pyTelegramBotAPI==4.14.0
python-dotenv==1.0.0
```

---

## 🔐 Безопасность

- ✅ Все конфиденциальные данные зашифрованы (Base64)
- ✅ Админ-панель доступна только по Telegram ID
- ✅ Проверка оплаты вручную администратором
- ✅ Капча защищает от ботов
- ✅ Режим тех.работ для безопасного обслуживания

---

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Посмотрите логи: `tail -100 bot.log`
3. Создайте Issue на GitHub: https://github.com/vasaa333/final/issues

---

## 📄 Лицензия

MIT License - используйте свободно для личных и коммерческих целей.

---

## ✨ Changelog

### v1.0.0 (Final Release)
- ✅ Полный функционал магазина
- ✅ Админ-панель с 7 разделами
- ✅ Капча с математическим вопросом
- ✅ Режим тех.работ с middleware
- ✅ Просмотр логов (последние 70 строк)
- ✅ Экспорт/импорт настроек
- ✅ Модерация отзывов
- ✅ Группировка товаров с остатками
- ✅ Все SQL ошибки исправлены

---

**Создано с ❤️ для эффективной работы интернет-магазина**
